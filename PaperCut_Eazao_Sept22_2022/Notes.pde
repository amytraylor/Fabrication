/*
float nozzleSizeClay = 1.5;
 float layerHeightClay = nozzleSizeClay;
 float startingZ =layerHeightClay*2;
 float pathWidth = nozzleSizeClay;
 float extrudeRateClay = 1.0;
 float extrusionForm = extrudeRateClay;
 float extrusionBase = extrusionForm/5;
 int claySpeed = 1250;
 
 int numPtsPerLayer = 180; //180 also works
 float heightVessel = 100;//mm//was 80
 int numLayers = int(heightVessel/layerHeightClay);
 float radius =30;//was 40
 //for nonplanar
 float numHumps = 1;
 
 float minScale = 0.85;
 float maxScale = 1.0;
 mindetail = 7
 
 mapWallDist = map(y, 0, img.height, 0.995, 0.98);
 
 else if (chosenForm==5) {
 currentRad = smoothStep(img, inc, 0, 255, 0, radius);
 
 
 */
